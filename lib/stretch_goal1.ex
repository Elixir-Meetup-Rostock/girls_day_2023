defmodule StretchGoal1 do
  alias MainTask

  #
  # lässt die Funktion roll_dices und print_results hintereinander aus führen
  #
  def run() do
  end

  #
  # gibt die Werte die übergeben wurden getrennt durch ein
  # leerzeichen in der Konsole aus
  #
  def print_results(values) do
    values
  end

  #
  # wirft 3 Würfeln
  # die Augenzahl der Würfel soll 1-6 sein
  #
  def roll_dices(range, count) do
    range
  end
end
